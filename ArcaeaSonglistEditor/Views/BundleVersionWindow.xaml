<Window x:Class="ArcaeaSonglistEditor.Views.BundleVersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArcaeaSonglistEditor.Views"
        mc:Ignorable="d"
        Title="Bundle版本信息"
        Height="300"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <TextBlock Grid.Row="0"
                   Text="请输入Bundle版本信息"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0,0,0,15" />
        
        <!-- 应用程序版本 -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="应用程序版本 (applicationVersionNumber)" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5" />
            <TextBox x:Name="AppVersionTextBox"
                     Text="1.0.0"
                     Height="25"
                     Padding="5" />
            <TextBlock Text="格式: X.Y.Z (例如: 6.11.9)"
                       FontSize="11"
                       Foreground="Gray" />
        </StackPanel>
        
        <!-- Bundle版本 -->
        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Bundle版本 (versionNumber)" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5" />
            <TextBox x:Name="BundleVersionTextBox"
                     Text="1.0.0"
                     Height="25"
                     Padding="5" />
            <TextBlock Text="格式: X.Y.Z (例如: 6.11.9.1)"
                       FontSize="11"
                       Foreground="Gray" />
        </StackPanel>
        
        <!-- 前一个Bundle版本 -->
        <StackPanel Grid.Row="3" Orientation="Vertical" Margin="0,0,0,15">
            <TextBlock Text="前一个Bundle版本 (previousVersionNumber)" 
                       FontWeight="SemiBold"
                       Margin="0,0,0,5" />
            <TextBox x:Name="PreviousVersionTextBox"
                     Height="25"
                     Padding="5" />
            <TextBlock Text="留空表示首次打包"
                       FontSize="11"
                       Foreground="Gray" />
        </StackPanel>
        
        <!-- 说明 -->
        <Border Grid.Row="4"
                BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15"
                Background="#FFF5F5F5">
            <TextBlock TextWrapping="Wrap">
                <Run Text="版本信息说明:" FontWeight="Bold" />
                <LineBreak />
                <Run Text="• 应用程序版本: 此Bundle适用的客户端版本" />
                <LineBreak />
                <Run Text="• Bundle版本: 此Bundle的版本号，应递增" />
                <LineBreak />
                <Run Text="• 前一个版本: 用于增量更新，首次打包留空" />
            </TextBlock>
        </Border>
        
        <!-- 按钮 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton"
                    Content="取消"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click" />
            <Button x:Name="OkButton"
                    Content="确定"
                    Width="80"
                    Height="30"
                    IsDefault="True"
                    Click="OkButton_Click" />
        </StackPanel>
    </Grid>
</Window>

